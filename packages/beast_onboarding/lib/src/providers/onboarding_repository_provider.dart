import 'package:beast_onboarding/src/repository/onboarding_repository.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

final onboardingRepositoryProvider = Provider<DefaultOnboardingRepository>((
  ref,
) {
  throw UnimplementedError(
    'You must override `onboardingRepositoryProvider` in your app with a concrete implementation.',
  );
});
